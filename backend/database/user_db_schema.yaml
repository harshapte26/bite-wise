type: array
description: Schema for user_db.json
items:
  type: object
  properties:
    user_name:
      type: string
      description: Unique identifier for the user
    recipe_names:
      type: array
      items:
        type: string
      description: List of recipe names saved/associated with the user
  required:
    - user_name
    - recipe_names
